====================================================================

Just to save time for every Node-Express project.
A basic skeleton structure for creating a new project.

====================================================================

To run app execute below commands on home directory:

1. npm install
2. npm start

====================================================================